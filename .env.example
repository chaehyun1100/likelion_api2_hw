# Render에 배포한 Spring Boot 서버 주소를 넣습니다. (끝에 슬래시 / 붙이지 마세요)
# 이 파일을 복사해 .env 를 만든 뒤 실제 주소로 바꾸세요.  →  cp .env.example .env
#
# ⚠️ Netlify(https)에 배포하려면 서버 주소도 반드시 https 여야 합니다. (Mixed Content 방지)
VITE_API_BASE_URL=https://your-render-app.onrender.com
